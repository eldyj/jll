(if (= (def? include.math) 0) (
	(let include.math 1)

	(include std/base)

	(let factorial n rest
		(if (zero? n)
				1
				(* n
					 (factorial (dec n)))))

	(let triangular n rest
		(if (zero? n)
				0
				(+ n
					 (triangular (dec n)))))

	(let fibonnacci n rest
		(if (or (zero? n)
						(one? n))
				n
				(+ (fibonnacci (dec n))
					 (fibonnacci (- n 2)))))

	(let pow n1 n2 rest
		(if (zero? n1
				0
				(if (zero? n2)
						1
						(* n1 (pow n1 (dec n2)))))))

))
